# Core runtime
requests>=2.31.0
feedparser>=6.0.11
langdetect>=1.0.9
jinja2>=3.1.4

# Sentiment model (optional — falls back to keyword classifier if absent)
torch>=2.2.0
transformers>=4.40.0

# Social media APIs (all optional — crawler skips gracefully if absent)
tweepy>=4.14.0           # Twitter / X
praw>=7.7.1              # Reddit
google-api-python-client>=2.120.0  # YouTube
